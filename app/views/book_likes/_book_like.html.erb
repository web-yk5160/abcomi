<% if current_user.liked_by?(@book.id) %>
    <%= link_to 'いいねを外す', destroy_book_like_path(@book), method: :DELETE, remote: true %> <%= @book.book_likes.count %>
<% else %>
    <%= link_to 'いいね', create_book_like_path(@book), method: :POST, remote: true %> <%= @book.book_likes.count %>
<% end %>
