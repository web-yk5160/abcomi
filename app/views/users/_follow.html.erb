<%= form_with(model: current_user.active_relationships.build, data: { remote: true }) do |f| %>
    <div><%= hidden_field_tag :followed_id, @user.id %></div>
    <%= f.button type: "submit", class: "follow-btn" do %>
        <i class="fas fa-users"></i> フォローする
    <% end %>
<% end %>
