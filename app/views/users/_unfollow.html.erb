<%= form_with(model: current_user.active_relationships.find_by(followed_id: @user.id),
            html: { method: :delete }, data: { remote: true }) do |f| %>
    <%= f.submit "フォロー解除する", class: "btn btn-primary" %>
<% end %>
